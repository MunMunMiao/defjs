---
title: Проектные решения
description: Почему Defjs использует явные клиенты, кортежи с учётом транспорта, опции жизненного цикла при выполнении, проекции в build и наблюдателей.
---

# Проектные решения

Здесь объясняются причины устройства текущего API. Поля и значения по умолчанию описаны на справочных страницах.

## Явные клиенты

В Defjs нет общего клиента по умолчанию на весь процесс. `createClient(...)` делает владельца видимым в месте вызова и позволяет приложению создавать разные клиенты для разных эндпоинтов, учётных данных, тестов или областей запроса.

У этой изоляции есть пределы. Перехватчики и колбэки опций могут замыкать общее состояние приложения, поэтому два объекта клиента не изолированы автоматически от всего окружения. Кроме того, `setErrorMap(...)` действует на весь процесс. Серверный код должен создавать клиент в области запроса, если опции или замыкания содержат данные запроса, пользователя, арендатора, cookie или авторизации.

Явный клиент упрощает разговор о владении ресурсами, но сам не управляет ими. Он не отслеживает и не освобождает активные HTTP-запросы, хендлы SSE или сеансы WebSocket.

## Кортежи с учётом транспорта

Все поддерживаемые команды возвращают трёхэлементный кортеж с ошибкой на первом месте, но третий элемент сохраняет смысл конкретного транспорта:

```typescript
const [httpError, data, response] = await client.execute(httpCommand)
const [sseError, stream, startupOpen] = await client.execute(sseCommand)
const [socketError, session, startupConnection] = await client.execute(socketCommand)
```

Так обёртка HTTP-ответа, снимок открытия SSE при запуске и снимок подключения WebSocket при запуске не сводятся к одной расплывчатой абстракции. Второй элемент устроен так же: HTTP возвращает декодированные данные, SSE — логический хендл потока, WebSocket — логический сеанс.

Кортеж явно представляет ожидаемые ошибки запуска и не навязывает управление через исключения. Но это не обещание, что произвольные перехватчики, колбэки, слушатели или неподдерживаемые значения никогда не приведут к отклонению Promise или синхронному исключению.

## Опции жизненного цикла задаются при выполнении

Описание эндпоинта фиксирует стабильный сетевой контракт и пределы транспортных очередей. Отмена, тайм-аут, heartbeat и переподключение относятся к конкретному выполнению, которое владеет работой.

HTTP и SSE принимают опции отмены при выполнении. WebSocket также принимает для отдельного запуска `beforeConnect`, heartbeat, переподключение и протоколы. Опции клиента задают переиспользуемые значения по умолчанию там, где транспорт это поддерживает; входная и выходная ёмкость WebSocket остаётся в определении эндпоинта.

Так одну команду можно переиспользовать. Фоновая задача и интерактивный экран выполняют её с разным временем жизни, не переопределяя путь или схему сообщений.

## `build` работает с проекциями

Пользовательский `build(request, input)` получает декларативное представление привязок, построенное из входного Struct. Значения, переданные вызывающим кодом, ему недоступны.

Это представление хранит соответствие исходных полей параметрам пути, query, заголовкам и телу. Модель поддерживает выбор полей, явные сетевые ключи и проекцию массива один к одному. Она намеренно не позволяет ветвиться по значениям, выполнять произвольные преобразования или добавлять литералы в проекцию.

Так построение запроса остаётся привязано к полям объявленного Struct. Нормализацию и бизнес-проверки выполняйте до создания команды. Поддерживаемые формы проекций перечислены в разделе [«Команды»](/ru-RU/core/commands).

## Наблюдатели не управляют потоком выполнения

SSE `onInvalidEvent` наблюдает за отброшенными событиями. Выброшенные ошибки и отклонённые Promise изолируются от потока управления stream, поэтому обработка продолжается. Асинхронного наблюдателя всё же ждут, поэтому он может задержать следующие сообщения.

Слушатели состояния и ошибок WebSocket во время выполнения тоже являются наблюдателями. Выброшенные ошибки и отклонённые Promise изолируются: ошибки слушателя состояния передаются слушателям runtime error, ошибки последних — в глобальный `reportError`, если он доступен; остальные слушатели и работа жизненного цикла продолжаются.

Решения по жизненному циклу принимайте через возвращённый хендл или сеанс. Наблюдатели подходят для ограниченного логирования, метрик и обновления состояния; удаляйте их при уничтожении владельца.

## Развёртывание sourcemap

Явно выберите политику production sourcemap:

- **public**: разверните map вместе с bundle. Map содержит `sourcesContent`, поэтому исходники приложения и зависимостей доступны публично даже при относительных source paths.

- **hidden**: удалите source-map reference из bundle, приватно загрузите map в платформу ошибок и не публикуйте её. Файл map по-прежнему содержит чувствительные пути и `sourcesContent`; «hidden» не делает его безопасным.

- **disabled**: не создавайте production map. Это исключает раскрытие map, но лишает source-level symbolication production stack и затрудняет debugging.

Ограничьте доступ и срок хранения private map как для других debugging artifacts. Относительные пути сами по себе не являются границей конфиденциальности.

## Граница OpenAPI

Выберите один авторитетный источник контракта. Организации с существующим OpenAPI workflow следует сохранить его и использовать mature generator вместе с явным runtime validator на границе приложения; сгенерированные TypeScript-типы сами не проверяют responses в runtime. Для greenfield Defjs service задавайте wire contract напрямую через Defjs Structs и endpoint definitions.

Core не будет добавлять OpenAPI generator/exporter или поддерживать OpenAPI и Defjs как два синхронизируемых источника. Dual-source drift хуже композиции существующих инструментов на чёткой границе.

## Справочные разделы

- [Клиент](/ru-RU/core/client) — композиция опций и область клиента.
- [Ошибки](/ru-RU/core/errors) — ошибки в кортежах и доступность ответа.
- [SSE](/ru-RU/core/sse) и [WebSocket](/ru-RU/core/web-socket) — логические хендлы, физические попытки и окончательное закрытие.
